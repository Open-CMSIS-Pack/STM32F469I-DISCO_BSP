# A solution is a collection of related projects that share same base configuration.
solution:
  created-for: CMSIS-Toolbox@2.5.0
  cdefault:

  # List the packs that define the device and/or board.
  packs:
    - pack: Keil::STM32F4xx_DFP@>=3.0.0-0
    - pack: Keil::STM32F469I-DISCO_BSP@>=1.0.0-0
    - pack: ARM::CMSIS-Driver_STM32@>=1.0.0-0
    - pack: ARM::CMSIS@>=6.0.0
    - pack: ARM::CMSIS-RTX@>=5.9.0
    - pack: ARM::CMSIS-Compiler@>=2.0.0

  # List different hardware targets that are used to deploy the solution.
  target-types:
    - type: STM32F469I-DISCO
      board: STMicroelectronics::STM32F469I-DISCO
      device: STMicroelectronics::STM32F469NIHx

  # List of different build configurations.
  build-types:
    - type: Debug
      debug: on
      optimize: debug

    - type: Release
      debug: off
      optimize: balanced

  # List related projects.
  projects:
    - project: Blinky.cproject.yml
